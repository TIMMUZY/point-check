export const avatarUrlSelector = (store) => store.avatar.avatarUrl;

export const avatarIdSelector = (store) => store.avatar.avatarId

export const userSelector = (store) => store.auth.user

export const territoriesAllSelector = (store) => store.territory.territoriesAllList

export const territoriesUserSelector = (store) => store.territory.territoriesUserList

export const isPassChangeSelector = (store) => store.isChange.isPassChange
